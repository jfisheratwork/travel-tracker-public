# Plan - Session 7

We are adding the example file `travel_tracker_backup_2026-05-23.json` to the "Load Example Data" dropdown in the application so that users on GitHub Pages (which does not support dynamic directory listing) can load it as a preloaded template.

## Objectives
1. Update `populateExamplesDropdown` in `docs/js/app.js` to include both `family1.json` and `travel_tracker_backup_2026-05-23.json` as dropdown options.
2. Update `populateExamplesDropdown` in `docs/preloaded_example.html` to include both options as well.
3. Verify that the files exist in `docs/examples/` and that the application builds and tests pass cleanly.
